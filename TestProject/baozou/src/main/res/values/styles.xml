<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#FB3D4F</item>
        <item name="colorPrimaryDark">#FB3D4F</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colortoolbar">#FB3D4F</item>
    </style>

    <style name="appLauncher" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="smallTextSize" parent="AppTheme">
        <item name="android:textSize">15dp</item>
    </style>
    <style name="bigTextSize" parent="AppTheme">
        <item name="android:textSize">20dp</item>
    </style>
    <style name="DayTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colortoolbar">@color/colortoolbar</item>
        <item name="background_color">@color/backgroud_red</item>
    </style>
    <style name="NightTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colortoolbar">@color/colortoolbar</item>
        <item name="background_color">@color/backgroud_red</item>
    </style>

    <!--自定义dialog背景白色背景无边框theme -->
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
